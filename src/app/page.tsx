import HeaderLayout from "@/layouts/HeaderLayout";
import React from "react";

type Props = {};

const HomePage = (props: Props) => {
  return <HeaderLayout>HomePage</HeaderLayout>;
};

export default HomePage;
